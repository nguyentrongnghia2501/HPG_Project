<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">

    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <% if current_user %>
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="#">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/blogs/new">New BLog</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">Pricing</a>
          </li>
        <% end %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Settings
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            <% if current_user %>
              <li>
                <%= button_to "sign_out", destroy_user_session_path, method: :delete, class: "btn btn-danger" %>
              </li>
            <% else %>
              <li>
                <%= button_to "sign_in", new_user_session_path, class: "btn btn-danger" %>
              </li>
              <li>
                <%= button_to "sign_up", new_user_registration_path,  method: :get, class: "btn btn-danger"  %>
              </li>
            <% end %>
            </ul>
          </li>
          <%= search_form_for @q, url: root_path, class: "d-flex" do |f| %>
            <%= f.search_field :title_cont, class: "form-control me-2", placeholder: "Search", aria: { label: "Search" } %>
            <%= f.submit "Search", class: "btn btn-outline-success", type: "submit" %>
          <% end %>
        </ul>
    </div>
  </div>
</nav>
